import React from 'react';
import styled from 'styled-components';

const FooterStyles = styled.footer`
    text-align: center;
    padding: 1rem 0;
`;

export const Footer = () => {
    return (
        <FooterStyles>
            Built by Tyler Henry's little typing fingers
        </FooterStyles>
    )
}
