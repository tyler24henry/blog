import styled from 'styled-components';

export const StyledRightNavHeader = styled.div`
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--grey);
    color: var(--black);
    font-size: 1.6rem;
    font-weight: 600;
    margin-bottom: 0.2rem;
`;